<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {},
            }
        }
    </script>
</head>
<body class="bg-white-900 text-white">
    <!-- Include Navigation Bar -->
    <%- include('../partials/nav') %>

    <!-- Registration Form -->
    <div class="max-w-md mx-auto mt-10 px-5">
        <div class="bg-gray-900 p-8 rounded-lg shadow-lg">
            <h2 class="text-3xl text-white font-bold text-center mb-6">Log In</h2>
            <form id="register-form" action="/login" method="POST">
                <div class="space-y-6">
                    <div>
                        <label for="email" class="block text-sm font-medium mb-1">Email</label>
                        <input type="email" id="email" name="email" class="w-full px-4 py-2 bg-gray-800 text-white border border-gray-700 rounded-md focus:outline-none focus:ring-2 focus:ring-[#4A3BFF]" required>
                    </div>
                    <div>
                        <label for="password" class="block text-sm font-medium mb-1">Password</label>
                        <input type="password" id="password" name="password" class="w-full px-4 py-2 bg-gray-800 text-white border border-gray-700 rounded-md focus:outline-none focus:ring-2 focus:ring-[#4A3BFF]" required>
                    </div>
                    <div>
                        <button type="submit" class="w-full flex items-center justify-center rounded-md bg-[#4A3BFF] text-white px-6 py-2.5 font-semibold hover:shadow-lg hover:drop-shadow transition duration-200">Login</button>
                    </div>
                </div>
            </form>
            <p class="mt-4 text-center text-sm">
                Don't have an account? <a href="/register" class="text-[#4A3BFF] hover:underline">Register</a>
            </p>
        </div>
    </div>
</body>
</html>