<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= blog.title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50 font-sans text-gray-800">
  <%- include("../partials/nav") %>

  <section class="max-w-4xl mx-auto px-4 py-16">

    <!-- Blog Info -->
    <h1 class="text-4xl font-bold text-gray-800 mb-4"><%= blog.title %></h1>
    <p class="text-lg text-gray-500 italic mb-6"><%= blog.subtitle %></p>
    <p class="text-gray-700 leading-loose mb-8"><%= blog.description %></p>

    <!-- Action Buttons -->
    <div class="flex gap-4">
      <a href="/editBlog/<%= blog.id %>"
         class="inline-block bg-yellow-500 hover:bg-yellow-600 text-white font-semibold px-4 py-2 rounded-md transition">
        Edit
      </a>

      <form action="/deleteBlog/<%= blog.id %>" method="POST">
        <button type="submit"
                class="bg-red-600 hover:bg-red-700 text-white font-semibold px-4 py-2 rounded-md transition">
          Delete
        </button>
      </form>
    </div>
  </section>
</body>

</html>
